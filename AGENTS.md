# Agent Instructions

This repository always uses [Conventional Commits](https://www.conventionalcommits.org/) for commit messages (e.g. `feat: ...`, `fix: ...`, `chore: ...`). Any commit created by an agent must follow this convention.

## Pull request reviewers

- When `lasl321` creates a pull request, add `9AR9` as a reviewer.
- When `9AR9` creates a pull request, add `lasl321` as a reviewer.
